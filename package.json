{
  "name": "telda-investing-tracker",
  "version": "0.1.0",
  "private": true,
  "description": "Zero-dependency portfolio dashboard for Telda / EGX investing",
  "type": "module",
  "engines": {
    "node": ">=22.6.0"
  },
  "scripts": {
    "check": "tsc --noEmit",
    "test": "node --experimental-strip-types --test \"src/**/*.test.ts\" \"scripts/**/*.test.mjs\"",
    "build": "node scripts/build.mjs",
    "serve": "node scripts/serve.mjs",
    "start": "npm run build && npm run serve",
    "verify": "npm run check && npm test",
    "deploy": "npm run build && npx --yes wrangler@latest pages deploy dist",
    "cf:dev": "npm run build && npx --yes wrangler@latest pages dev dist"
  }
}
